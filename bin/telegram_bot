#!/usr/bin/env ruby

begin
  require_relative '../config/environment'
  Telegram::Bot::UpdatesPoller.start(:chat, TelegramWebhookController)
rescue Exception => e
  Sentry.capture_exception(e)
  Rollbar.report_exception(e) if defined?(Rollbar) && !e.is_a?(SystemExit)
  raise
end
