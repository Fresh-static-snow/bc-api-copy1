en:
  activerecord:
    exceptions:
      not_found: "Couldn't find %{model_name} with 'id'=%{id}"
    errors:
      models:
        tournament:
          attributes:
            start_at:
              invalid_datetime: "The start date and time are invalid."
            end__at:
              invalid_datetime: "The end date and time are invalid."
        match:
          attributes:
            start_at:
              invalid_datetime: "The start date and time are invalid."
            end__at:
              invalid_datetime: "The end date and time are invalid."
        corporate:
          attributes:
            start_at:
              invalid_datetime: "The start date and time are invalid."
            end__at:
              invalid_datetime: "The end date and time are invalid."
  users:
    properties:
      id: Unique number that identifies a user
      email: User email address
      display_name: User full name
    params:
      email: User email address
      password: 'Length should be 8-16 characters and include at least: 1 uppercase, 1 lowercase, 1 digit and 1 special character'
      password_confirmation: Should be exactly the same as password
      set_password_token: Token generated by the system to reset existing password
    errors:
      password_complexity: 'Complexity requirement not met. Length should be 8-16 characters and include at least: 1 uppercase, 1 lowercase, 1 digit and 1 special character'
      current_password: Current password is invalid
      same_password: New password shouldn't be same as old Password
      revoked_access: Your access has been revoked. Please, contact the administration
  sessions:
    errors:
      invalid_email_or_password: Invalid Email or Password.
